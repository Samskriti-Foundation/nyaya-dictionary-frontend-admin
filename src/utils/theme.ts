import { extendTheme } from "@chakra-ui/react";

const colors = {
  primary: "#AD341C",
  secondary: "#DA6619",
  background: "#FFF7F1",
  foreground: "#FFFFFF",
};

const theme = extendTheme({ colors });

export default theme;